/*
export async function load(context) {
    const res = await context.fetch('http://127.0.0.1:8080/api/agents');
    const agents_data = await res.json();
    const agents = agents_data.agents;
    return {
        agents
    }
}*/
